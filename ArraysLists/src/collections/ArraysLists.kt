package collections

fun main() {
    val array = arrayOf("Texas", "Iowa", "California")

    // we should use if we want to add and delete items
    val list = listOf("Orange", "Apple", "Bananas") // this is immutable

    list.

    // a list that internally create an array
    val arrayList = arrayListOf("Patrick", "Michael", "Sarah")
}